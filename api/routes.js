/**
 * IMPORTANDO AS ROTAS
 */
const rotaCardapio = require('./routes/route-cardapio');
const rotaCardapio = require('./routes/route-cardapio');

/**
 * USANDO OU CRIANDO AS ROTAS
 */
app.use('/pagamento/',rotaPagamento);

module.exports = rotaCardapio